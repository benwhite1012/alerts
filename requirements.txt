# Core HTTP + async
requests==2.32.3
aiohttp==3.9.5

# AWS SDK
boto3==1.34.160

# OpenAI SDK (you’re using the new client)
openai>=1.35.0

# Env loader
python-dotenv==1.0.1

# YouTube transcripts
youtube-transcript-api==0.6.2

# Whisper speech-to-text (uses PyTorch)
openai-whisper==20231117
torch==2.3.1      # CPU build; works with whisper
